# Bluetooth
sudo apt install -y blueman pavucontrol

# Logitech
sudo apt install -y solaar

# Wallpaper
sudo apt install -y wallch

# Gnome
sudo apt install -y gnome-tweaks gnome-shell-extensions

# Taskwarrior
sudo apt install -y taskwarrior

# VSCode
sudo snap install code --classic

sudo /usr/bin/gsettings set org.gnome.shell favorite-apps "['firefox.desktop', 'org.gnome.Nautilus.desktop', 'org.gnome.Terminal.desktop', 'code_code.desktop']"


